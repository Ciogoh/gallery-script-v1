function setup() {
  createCanvas(800, 600);
  background(255);

  stroke(0);
  strokeWeight(2);

  let spacing = 20;

  for (let y = spacing; y < height; y += spacing) {
    line(0, y, width, y);
  }

  // Salva il canvas come PNG
  save("linee_orizzontali.png");

  noLoop();
}

function draw() {
}
